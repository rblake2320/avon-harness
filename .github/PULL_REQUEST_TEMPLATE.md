## What

## Why

## Checklist
- [ ] `cd backend && python -m pytest` passes
- [ ] `cd web && npm run build` passes
- [ ] New endpoints have tenant-isolation tests
- [ ] No secrets in diff (keys, tokens, .env values)
