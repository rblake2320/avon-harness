# --- Core ---
DATABASE_URL=postgresql+psycopg://mk:mk@db:5432/mkharness
JWT_SECRET=change-me-64-random-bytes
MASTER_KEY=   # base64 32 bytes. generate: python -c "import os,base64;print(base64.b64encode(os.urandom(32)).decode())"
CORS_ORIGINS=http://localhost:5173

# --- Central gateway provider keys (company-held tier). Blank = BYO required for that provider. ---
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
OLLAMA_BASE_URL=http://localhost:11434

# --- Limits ---
RATE_LIMIT_PER_MINUTE=30
MAX_UPLOAD_MB=8
